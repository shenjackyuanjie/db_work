# 柑橘果园智能诊断与管理系统报告计划书

## 1. 报告定位

本报告面向课程设计、项目验收或毕业设计阶段的系统说明文档，目标是完整说明本项目的建设背景、需求分析、总体架构、核心功能、数据库设计、接口设计、推理流程、部署方式和测试验证结果。

报告应避免写成单纯的代码说明书。重点应放在以下问题：

- 系统要解决什么业务问题。
- 系统由哪些模块组成。
- 每个模块如何支撑业务目标。
- 数据如何流转、存储和使用。
- AI 推理能力如何接入系统。
- 系统如何部署、运行和验证。

## 2. 报告题目建议

可选题目：

- 《柑橘果园智能诊断与管理系统设计与实现》
- 《基于 Rust 与 ONNX 的柑橘病害识别管理系统设计》
- 《面向柑橘果园场景的智能诊断与任务管理平台设计与实现》

推荐使用第一项，覆盖范围最完整。

## 3. 写作目标

报告应达到以下目标：

1. 清楚说明系统建设背景和应用价值。
2. 明确区分普通用户、管理员和系统运行维护方的功能需求。
3. 给出系统总体架构、模块划分和关键技术选型。
4. 说明 Rust 后端、PostgreSQL 数据库、ONNX 推理、OpenRouter AI 能力和前端静态页面之间的协作关系。
5. 展示核心功能的实现逻辑，包括用户认证、病害识别、温湿度采样、任务管理、管理员后台和 3D 果园沙盘。
6. 给出数据库表结构、接口分组、部署步骤和测试方案。
7. 总结当前系统不足，并提出后续优化方向。

## 4. 资料依据

报告写作应主要参考以下项目文件：

- `README.md`：项目能力、运行方式、接口概览、数据表和项目结构。
- `src/main.rs`：服务入口和配置加载流程。
- `src/config.rs`：配置文件结构。
- `src/server.rs`：主路由注册、静态资源托管和服务启动逻辑。
- `src/server/bootstrap.rs`：数据库自动建表和示例果园数据初始化逻辑。
- `src/server/handlers_core/`：页面、看板、温湿度、任务和识别记录接口。
- `src/server/handlers_ai/`：病害识别、AI 分析和施肥建议生成逻辑。
- `src/user_routes/`：登录、注册、会话、管理员和审批接口。
- `src/inference/`：ONNX 本地推理和远端混合推理。
- `src/client/`：OpenRouter 客户端和施肥建议调用。
- `static/`：首页、识别页、管理员后台和 3D 沙盘页面。

注意：报告中不要写入真实数据库连接串、真实 OpenRouter API Key 或其他敏感配置。

## 5. 报告章节计划

### 5.1 摘要

说明系统目的、核心技术和实现结果。

建议包含：

- 柑橘果园病害识别和管理场景。
- Rust + Axum 后端服务。
- PostgreSQL 数据存储。
- ONNX 本地推理与 OpenRouter 远端 AI 分析。
- 用户端、管理员端和 3D 沙盘能力。

### 5.2 项目背景与意义

写作重点：

- 柑橘种植过程中病害识别、环境监测和任务管理的重要性。
- 传统人工巡检存在响应慢、记录分散、管理成本高等问题。
- 智能诊断系统可以提升病害发现、治理任务生成和果园数据管理效率。

### 5.3 需求分析

按角色拆分。

普通用户需求：

- 注册、登录、登出和会话校验。
- 上传叶片图片进行病害识别。
- 查看识别历史记录。
- 查看温湿度数据、健康点和任务列表。
- 查看 3D 果园沙盘。
- 生成施肥建议。

管理员需求：

- 管理用户权限。
- 创建和查询邀请码。
- 审核待注册用户。
- 管理系统设置。
- 查看仪表盘统计和审计日志。
- 查看指定用户的果园总览数据。

系统需求：

- 支持 PostgreSQL 持久化。
- 支持启动时自动建表。
- 支持本地 ONNX 模型推理。
- 支持远端 AI 分析和施肥建议生成。
- 支持前后端同端口部署。

### 5.4 系统总体设计

建议从三层结构描述：

- 表现层：`static/` 中的 HTML、CSS、JavaScript 页面，包括首页、识别页、管理员后台和 3D 沙盘。
- 服务层：Rust + Axum 后端，负责路由、认证、业务处理、AI 调用和静态文件托管。
- 数据层：PostgreSQL，保存用户、会话、识别记录、温湿度、任务、系统设置、果园树位和传感器记录。

建议插入架构图：

```text
浏览器前端
  |
  | HTTP API / 静态页面
  v
Axum 后端服务
  |
  |-- 用户与会话模块
  |-- 病害识别模块
  |-- 温湿度与任务模块
  |-- 管理员后台模块
  |-- 果园沙盘数据模块
  |
  |-- PostgreSQL
  |-- ONNX 模型
  |-- OpenRouter AI 服务
```

### 5.5 技术选型

建议说明每项技术的作用：

- Rust 2024：系统主要开发语言。
- Tokio：异步运行时。
- Axum：HTTP API 和 Web 服务框架。
- SQLx：PostgreSQL 数据访问。
- Tract ONNX：本地 ONNX 模型推理。
- Reqwest：调用 OpenRouter 等外部 HTTP 服务。
- Serde：JSON 和 TOML 序列化、反序列化。
- Tower HTTP：CORS、静态文件托管和中间件支持。
- Three.js：前端 3D 果园沙盘渲染。

### 5.6 功能模块设计与实现

建议分模块写，每个模块包含“功能说明、关键接口、主要数据表、实现要点”。

用户认证模块：

- 注册、登录、登出、token 校验。
- 普通用户和管理员权限区分。
- 相关表：`app_users`、`app_sessions`、`app_pending_users`、`app_invitations`。

病害识别模块：

- 支持 `/api/citrus-disease` 和 `/api/citrus-disease-v2`。
- 支持图片上传、Base64 图片、温湿度参数和区域参数。
- 识别结果写入 `app_diagnosis_records`。
- 识别图片归档到 `static/uploads`，通过 `/media/recognition_records/*` 访问。

推理与 AI 模块：

- `onnx` 模式：本地完成果树识别和病害分类。
- `remote` 模式：先用本地模型判断是否为果树，再调用 OpenRouter 生成高级分析。
- OpenRouter 还用于柑橘图像分析和施肥建议生成。

环境监测模块：

- 记录普通温湿度采样。
- 写入带标签序列号的树传感器记录。
- 支撑果园健康点和沙盘数据展示。

任务管理模块：

- 支持任务查询、手动新增和完成。
- 支持根据病害和环境风险自动生成治理任务。

管理员模块：

- 用户管理。
- 邀请码管理。
- 待审核用户审批与驳回。
- 系统设置读取与更新。
- 仪表盘统计和审计日志。

果园沙盘模块：

- 后台页面展示二维果园地图。
- `orchard-3d.html` 展示 Three.js 3D 沙盘。
- 沙盘数据来自 `app_orchard_trees`、`app_tree_sensor_records` 和 `app_diagnosis_records`。

### 5.7 数据库设计

核心表说明：

- `app_users`：用户账号、管理员标识、经纬度等信息。
- `app_sessions`：登录会话 token。
- `app_invitations`：邀请码、使用状态和过期时间。
- `app_pending_users`：待审核注册用户。
- `app_tasks`：果园治理任务。
- `app_temperature_humidity`：温湿度采样数据。
- `app_diagnosis_records`：病害识别记录。
- `app_system_settings`：系统设置。
- `app_admin_audit_logs`：管理员审计日志。
- `app_orchard_trees`：果树编码、标签序列号、位置和地形高度。
- `app_tree_sensor_records`：树标签传感器采样记录。

需要说明：

- 当前项目采用服务启动时自动建表的方式。
- 当果树和传感器表为空时，系统会初始化一组演示树位和采样数据。
- 后续如果进入生产环境，建议引入独立迁移工具管理数据库结构演进。

### 5.8 接口设计

建议按接口分组，而不是逐个复制所有请求响应。

基础与会话：

- `GET /health`
- `POST /api/register`
- `POST /api/login`
- `POST /api/logout`
- `POST /api/validate`
- `POST /user/me`

病害识别与记录：

- `POST /api/citrus-disease`
- `POST /api/citrus-disease-v2`
- `POST /citrus/analyze`
- `GET /api/recognition-records`
- `GET /api/disease-treatment`

温湿度、任务和看板：

- `GET /api/temperature-humidity`
- `POST /api/temperature-humidity`
- `GET /api/tasks`
- `POST /api/tasks/add`
- `POST /api/tasks/complete`
- `GET /api/health-point`

果园与管理员：

- `POST /user/orchard/overview`
- `POST /user/admin/orchard/overview`
- `POST /user/admin/dashboard/stats`
- `POST /user/admin/dashboard/logs`
- `POST /user/admin/settings/get`
- `POST /user/admin/settings/update`

### 5.9 部署与运行设计

部署前提：

- 安装 Rust 1.85 或更新版本。
- 准备 PostgreSQL 数据库。
- 准备 ONNX 模型文件。
- 配置 OpenRouter API Key。
- 确认浏览器能访问 jsDelivr CDN，供 3D 沙盘加载 Three.js。

运行流程：

1. 修改 `config.toml`。
2. 准备数据库和模型文件。
3. 执行 `cargo run --release`。
4. 访问 `http://127.0.0.1:11000/index.html`。

报告中应使用脱敏配置示例：

```toml
[server]
addr = "0.0.0.0:11000"
log_level = "info"

[ai]
openrouter_api_key = "sk-or-v1-your-key"

[database]
postgres_url = "postgres://user:password@127.0.0.1:5432/db_name"

[inference]
mode = "onnx"
model_1_path = "onnx/model_1.onnx"
model_2_path = "onnx/model_2.onnx"
```

### 5.10 测试计划

功能测试：

- 注册普通用户。
- 使用邀请码注册管理员或普通用户。
- 登录、登出和 token 校验。
- 上传叶片图片进行病害识别。
- 查询识别历史记录。
- 提交温湿度采样。
- 新增、完成和自动生成任务。
- 管理员查看用户、审批用户、修改系统设置。
- 打开管理员后台和 3D 沙盘页面。

接口测试：

- 使用 curl 或浏览器开发者工具验证核心 API。
- 重点验证参数缺失、无效 token、普通用户访问管理员接口等异常情况。

数据测试：

- 验证服务启动后是否自动创建数据库表。
- 验证识别记录、温湿度记录、任务记录是否正确入库。
- 验证树位和传感器演示数据是否能支撑沙盘展示。

安全测试：

- 未登录访问 `/analyze.html` 应被重定向。
- 普通用户访问 `/admin.html` 应被重定向。
- 无效 token 调用受保护接口应返回错误。
- 报告和代码提交中不应包含真实密钥。

### 5.11 结果展示计划

建议准备以下截图或表格：

- 首页或登录页截图。
- 病害识别页面截图。
- 病害识别结果截图。
- 管理员后台截图。
- 3D 果园沙盘截图。
- 数据库核心表结构表。
- 核心 API 列表表格。
- 测试用例与测试结果表。

### 5.12 安全性与可维护性分析

可写内容：

- 基于 token 和 Cookie 的登录态校验。
- 管理员接口权限隔离。
- 系统设置支持维护模式和注册开关。
- 识别记录和审计日志可追踪关键操作。
- 配置文件集中管理数据库和 AI Key。

需要指出的不足：

- 当前数据库结构主要由启动逻辑自动创建，缺少正式迁移版本管理。
- 自动化测试覆盖还可以继续完善。
- 静态页面和后端服务耦合在同一服务内，后续可按部署需求拆分。
- 3D 沙盘依赖 CDN，离线环境需要本地化前端依赖。

### 5.13 总结与展望

总结应覆盖：

- 系统完成了柑橘病害识别、果园环境监测、任务管理、管理员后台和 3D 沙盘展示。
- 后端使用 Rust + Axum 实现，具备较好的性能和类型安全基础。
- 数据库使用 PostgreSQL，支持核心业务数据持久化。
- 推理层同时支持本地 ONNX 和远端 AI 分析。

后续展望：

- 引入数据库迁移工具。
- 增加更完整的自动化测试。
- 扩展更多病害类型和作物类型。
- 接入真实物联网传感器数据。
- 增加短信、邮件或站内告警通知。
- 优化 3D 沙盘的离线加载和渲染性能。

## 6. 写作进度安排

建议按 5 个阶段推进：

| 阶段 | 时间 | 任务 | 产出 |
| --- | --- | --- | --- |
| 第 1 阶段 | 第 1 天 | 阅读 README、梳理功能和技术栈 | 报告目录和资料清单 |
| 第 2 阶段 | 第 2 天 | 编写背景、需求分析、总体设计 | 第 1-4 章初稿 |
| 第 3 阶段 | 第 3 天 | 编写模块设计、数据库设计、接口设计 | 第 5-8 章初稿 |
| 第 4 阶段 | 第 4 天 | 编写部署、测试、安全和总结 | 完整初稿 |
| 第 5 阶段 | 第 5 天 | 补充截图、表格、图示并校对 | 最终报告 |

## 7. 图表清单

建议至少包含以下图表：

- 系统总体架构图。
- 用户使用流程图。
- 病害识别流程图。
- 数据库核心表说明表。
- 核心接口说明表。
- 测试用例结果表。
- 系统部署流程图。

病害识别流程图示例：

```text
用户上传图片
  |
  v
解析请求体和用户信息
  |
  v
第一阶段模型判断是否为果树
  |
  +-- 否 --> 返回非果树结果并记录
  |
  +-- 是 --> 本地病害分类或 OpenRouter 高级分析
              |
              v
           生成识别结果
              |
              v
           写入识别记录和图片归档
              |
              v
           必要时自动生成治理任务
```

## 8. 交付标准

最终报告建议满足以下标准：

- 总字数根据课程或验收要求控制，通常不少于 6000 字。
- 章节结构完整，标题层级清晰。
- 所有技术描述与当前项目代码保持一致。
- 配置示例已脱敏。
- 图表和截图能支撑文字说明。
- 测试结果能覆盖核心业务流程和主要异常流程。
- 总结部分明确说明系统已有成果和后续改进方向。

## 9. 注意事项

- 不要在报告中暴露真实 `config.toml` 内容。
- 不要把 README 直接复制为报告正文，需要扩展设计原因、模块关系和验证过程。
- 如果展示接口请求示例，应优先使用脱敏账号和本地地址。
- 如果展示数据库截图，应遮挡敏感字段。
- 如果项目后续代码继续变更，报告中的接口列表、数据表和运行方式需要同步更新。
